# ⚠️ DO NOT PUT REAL SECRETS IN THIS FILE
# This is a template for environment variables.
# Copy this file to `.env` and replace the placeholders with your actual credentials.
# The `.env` file is ignored by Git.

# ===============================
# 🔗 FRONTEND CONFIG (Next.js)
# ===============================

# Local or deployed frontend URL
# ❌ Must change per project/environment (localhost, staging, prod)
FRONTEND_URL=http://localhost:3000

# Stripe (public key for browser - safe to expose)
# ♻️ Can reuse if using same Stripe account/products
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_1234567890abcdef

# MailerLite (email capture)
# ♻️ Can reuse if using same MailerLite account
MAILERLITE_API_KEY=your-mailerlite-api-key

# OpenAI API key (for chatbot, AI captions, etc.)
# ♻️ Can reuse if using same OpenAI account
OPENAI_API_KEY=sk-your-openai-key-here


# ===============================
# 🚀 BACKEND CONFIG (FastAPI)
# ===============================

# Local or deployed backend URL
# ❌ Must change per project/environment
BACKEND_URL=http://localhost:8000

# Supabase (auth + DB)
# ❌ All must change per new Supabase project
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-service-role-or-anon-key
SUPABASE_JWT_SECRET=your-jwt-secret  # Used in auth + token validation
SUPABASE_DB_PASS=your-database-password

# Stripe (secure backend keys)
# ❌ Must match your Stripe account/environment (test/live)
STRIPE_SECRET_KEY=sk_test_1234567890abcdef
# ❌ Must change for each webhook endpoint you create
STRIPE_WEBHOOK_SECRET=whsec_abcdef1234567890

# Runtime environment + logging
# ❌ ENVIRONMENT may change per stage (dev/staging/prod)
ENVIRONMENT=dev                 # dev | staging | prod
# ♻️ Can reuse preferred logging level across projects
LOG_LEVEL=info                  # debug | info | warning | error


# ===============================
# 🛡️ OPTIONAL: MONITORING / PRO TOOLS
# ===============================

# Error monitoring (e.g., Sentry.io)
# ♻️ Can reuse if reporting all errors to same Sentry project
SENTRY_DSN=your-sentry-dsn

# Logtail (structured logging)
# ♻️ Can reuse if sending logs to same Logtail source
LOGTAIL_TOKEN=your-logtail-token


# ===============================
# 🧪 OPTIONAL: CI/CD / DEPLOYMENT
# ===============================

# GitHub Container Registry access (used in deploy script)
# ♻️ Can reuse for all repos if same GitHub account/org
GHCR_PAT=your-github-personal-access-token

# Railway project reference (if using CI/CD deploys)
# ❌ Must change for each new Railway project
RAILWAY_PROJECT_ID=your-railway-project-id
