[pytest]
# Your tests live here:
testpaths = tests

# So tests can import your FastAPI code:
pythonpath = backend/src

# Quiet the asyncio fixture warning:
asyncio_default_fixture_loop_scope = function

# Don’t recurse into envs or node_modules:
norecursedirs = .git .* venv .venv env backend/.venv node_modules frontend/node_modules dist build
